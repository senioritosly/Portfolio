import React from 'react'
import styles from './Card.module.css'
import PropTypes from 'prop-types'

const Card = () => {
  return (
    <div>Card</div>
  )
}

Card.propTypes = {
    glob: PropTypes.string
}

export default Card